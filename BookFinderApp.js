/* Goals: User can enter a search query into an input field
    User can submit the query. This will call an API that will return an
    array of books with the corresponding data.
    user can see the list of books appearing on the page.

    Optional Goals: For each item in the list add a link that will send the
    user to an external site which has more information about the book.
    Implement a Responsive Design.
    Add Loading Animations.
 API: https://www.googleapis.com/books/v1/volumes?q=search+terms */


